---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import PostCard from '../../../components/PostCard.astro';
import BlogHero from '../../../components/BlogHero.astro';
const posts = (await getCollection('posts', (p) => p.data.lang === 'en'))
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---
<BaseLayout title="News — Opendream" lang="en" path="/en/blogs" altPath="/blog">
  <BlogHero title="Press" subtitle="Where our stories are shared" />
  <section class="od-listing">
    <div class="od-container">
      <div class="od-postgrid">
        {posts.map((p) => <PostCard title={p.data.title} href={p.data.path} cover={p.data.cover} excerpt={p.data.excerpt} />)}
      </div>
    </div>
  </section>
</BaseLayout>
