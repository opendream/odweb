---
// Blog/press banner — now just the shared <Hero> with the purple bg.
import Hero from './content/Hero.astro';
interface Props { title: string; subtitle: string; }
const { title, subtitle } = Astro.props;
---
<Hero title={title} subtitle={subtitle} bg="#664f9e" image="/wp-content/uploads/2016/12/bg-head-blog.jpg" />
